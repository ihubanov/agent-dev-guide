# OpenAI API Key - REQUIRED
OPENAI_API_KEY="your_openai_api_key_here"

# OpenAI Model Name - Optional (defaults to gpt-4o)
# MODEL_NAME="gpt-4o"

# OpenAI Base URL - Optional (for proxies or other OpenAI-compatible APIs)
# OPENAI_BASE_URL="your_custom_openai_api_base_url"

# Base URL for the MCP server itself (this FastAPI application) - Optional
# This is used by the prompt client logic to call the tool endpoint.
# Defaults to http://localhost:8000 if not set.
# Ensure this matches where the server is actually running and accessible.
# MCP_SERVER_BASE_URL="http://localhost:8000"

# Disable thought logging to console - Optional (defaults to "false")
# Set to "true" to disable the detailed thought logging printed by mcp_server.py
# DISABLE_THOUGHT_LOGGING="false"
